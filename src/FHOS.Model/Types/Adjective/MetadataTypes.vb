Friend Module MetadataTypes
    Friend ReadOnly Subtype As String = NameOf(Subtype)
    Friend ReadOnly EntityName As String = NameOf(EntityName)
    Friend ReadOnly Recipient As String = NameOf(Recipient)
End Module
