Friend Module StatisticTypes
    Friend ReadOnly Authority As String = NameOf(Authority)
    Friend ReadOnly Conviction As String = NameOf(Conviction)
    Friend ReadOnly Column As String = NameOf(Column)
    Friend ReadOnly CurrentFuelCapacity As String = NameOf(CurrentFuelCapacity)
    Friend ReadOnly CurrentOxygenCapacity As String = NameOf(CurrentOxygenCapacity)
    Friend ReadOnly DestinationPlanetId As String = NameOf(DestinationPlanetId)
    Friend ReadOnly Facing As String = NameOf(Facing)
    Friend ReadOnly Fuel As String = NameOf(Fuel)
    Friend ReadOnly FuelCapacity As String = NameOf(FuelCapacity)
    Friend ReadOnly JoolsReward As String = NameOf(JoolsReward)
    Friend ReadOnly MinimumPlanetCount As String = NameOf(MinimumPlanetCount)
    Friend ReadOnly OriginPlanetId As String = NameOf(OriginPlanetId)
    Friend ReadOnly Oxygen As String = NameOf(Oxygen)
    Friend ReadOnly OxygenCapacity As String = NameOf(OxygenCapacity)
    Friend ReadOnly PlanetCount As String = NameOf(PlanetCount)
    Friend ReadOnly ReputationBonus As String = NameOf(ReputationBonus)
    Friend ReadOnly ReputationPenalty As String = NameOf(ReputationPenalty)
    Friend ReadOnly Row As String = NameOf(Row)
    Friend ReadOnly SatelliteCount As String = NameOf(SatelliteCount)
    Friend ReadOnly Scrap As String = NameOf(Scrap)
    Friend ReadOnly ShipyardCount As String = NameOf(ShipyardCount)
    Friend ReadOnly Standards As String = NameOf(Standards)
    Friend ReadOnly StarDockCount As String = NameOf(StarDockCount)
    Friend ReadOnly StarGateCount As String = NameOf(StarGateCount)
    Friend ReadOnly TradingPostCount As String = NameOf(TradingPostCount)
    Friend ReadOnly VisitCount As String = NameOf(VisitCount)
    Friend ReadOnly WormholeCount As String = NameOf(WormholeCount)
End Module
